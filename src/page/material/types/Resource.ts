export default interface Resource {
  id: string;
  name: string;
  enable: number;
  description: string;
  province: string;
  phone: string;
  email: string;
  county: string;
  city: string;
  address: string;
}
